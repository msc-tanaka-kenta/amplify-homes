type Pos @model @auth(rules: [{allow: public}]) {
  id: ID!
  timestamp: AWSTimestamp
  lat: Float
  lng: Float
  spd: Float
}

type Home @model @auth(rules: [{allow: public}]) {
  id: ID!
  address: String
  image_url: String
  price: Float
} 